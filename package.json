{
  "name": "unslackd",
  "version": "1.0.0",
  "description": "Bring Untappd info to Slack",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf build && mkdir build",
    "build-tsc": "tsc",
    "build": "npm run clean && npm run build-tsc",
    "start": "npm run build && node ./build/index.js",
    "dev": "npm run build && node -r dotenv/config ./build/index.js",
    "test": "mocha --require ts-node/register --bail --recursive --sort --full-trace ./test",
    "debug": "npm run build && node --inspect-brk -r dotenv/config ./build/index.js"
  },
  "dependencies": {
    "@slack/interactive-messages": "^2.0.2",
    "body-parser": "^1.19.2",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "node-fetch": "^3.2.1"
  },
  "engines": {
    "node": "16.14.0"
  },
  "author": "Harry Paterra",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.21",
    "chai": "^4.3.6",
    "eslint": "^8.10.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.25.4",
    "mocha": "^9.2.1",
    "ts-node": "^10.6.0",
    "typescript": "^4.6.2"
  }
}
